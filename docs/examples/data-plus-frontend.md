# Example: Data + Frontend

Common split:

- primary agent: data ingestion, source freshness, schemas, backend scripts
- secondary agent: dashboard clarity, UX, docs, presentation

Typical focus paths:

- data: `scripts`, `src/lib`, `prisma`, `data_store`, `docs`
- frontend: `src/app`, `src/components`, `docs`
