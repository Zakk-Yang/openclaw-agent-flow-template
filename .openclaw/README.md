# .openclaw

This folder holds the project-local OpenClaw configuration.

What lives here:

- `project.json`: project slug, timing, dispatch mode, and agent definitions
- `roles/`: role briefs injected into agent prompts
- `runtime/`: local runtime state and logs generated by the supervisor loop

Edit this folder when you want to change how the project's agent flow behaves.
