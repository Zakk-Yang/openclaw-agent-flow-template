# .openclaw

This folder holds the local OpenClaw settings for this project.

Think of it as the control room for the project.

- `project.json`: the project name, timing, and agent settings
- `roles/`: the instructions for each agent
- `runtime/`: logs and state files created while the workflow is running

Edit this folder when you want to change how the project behaves.
